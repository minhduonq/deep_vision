# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.23.0
opencv-python>=4.8.0
Pillow>=10.0.0
tqdm>=4.65.0
requests>=2.31.0
loguru>=0.7.0

# BasicSR - install from GitHub to avoid build issues
# Run: pip install git+https://github.com/XPixelGroup/BasicSR.git
# basicsr>=1.4.2  # Commented out - install manually from GitHub

# NAFNet dependencies (after basicsr)
einops>=0.7.0

# LaMa
simple-lama-inpainting>=0.1.0

# GFPGAN and dependencies
gfpgan>=1.3.8
facexlib>=0.3.0
realesrgan>=0.3.0

# HuggingFace
transformers>=4.30.0
diffusers>=0.21.0
accelerate>=0.20.0

# Optional (uncomment if needed)
# xformers  # For faster attention, requires specific CUDA version
# onnxruntime-gpu  # For ONNX model acceleration
